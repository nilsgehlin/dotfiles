# Keep Zsh lightweight

Oh My Zsh was removed because its broad feature set was mostly unused and made prompt loading feel slow. Keep the Zsh setup lightweight: prefer built-in Zsh functionality and small, explicit configuration, and add frameworks or plugins only when they provide a regularly used benefit that justifies their startup and maintenance cost.
