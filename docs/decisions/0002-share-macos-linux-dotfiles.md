# Share macOS and Linux dotfiles

Use one shared shell and tmux configuration on macOS and Linux rather than maintaining platform-specific copies. Derive user paths from `$HOME`, select only genuinely different tool locations by operating system, and use tmux's native clipboard integration so remote sessions can pass copied text through compatible terminals without requiring a graphical Linux clipboard utility.
